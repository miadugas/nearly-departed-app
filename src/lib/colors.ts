/** The user's dot on the map. Shared so the tab bar can echo it. */
export const YOU_BLUE = "#4c8dff";

// Mirror of the tailwind `accent` token, for places that need a raw string
// (icon `color` props, inline styles): the icon's electric green — the one
// chromatic accent on the monochrome palette; everything that used to be
// pink points here.
export const ACCENT = "#47ec51";
